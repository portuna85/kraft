# 빌드 캐시·병렬 실행을 켠다(개선 보고서 BLD-01) — 지금까지 기본값(둘 다 꺼짐)으로 돌아
# 매번 전체를 다시 컴파일·테스트했다.
org.gradle.caching=true
org.gradle.parallel=true
org.gradle.jvmargs=-Xmx2g

# ./gradlew test bootJar --configuration-cache를 두 번 연달아 돌려 문제 없이 캐시를 재사용하는
# 것을 확인한 뒤 켰다(개선 보고서 BLD-01).
org.gradle.configuration-cache=true
