package com.kraft.statistics;

import org.springframework.data.jpa.repository.JpaRepository;

public interface StatisticsProjectionStateRepository extends JpaRepository<StatisticsProjectionState, Long> {
}
