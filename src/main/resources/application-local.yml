spring:
  datasource:
    url: jdbc:mysql://localhost:3306/kraft_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: portuna85
    password: Gkstmvns1!
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  session:
    jdbc:
      initialize-schema: always   # 로컬은 자동 생성
logging:
  level:
    org.hibernate.SQL: debug