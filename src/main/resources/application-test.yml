spring:
  datasource:
    url: jdbc:mysql://localhost:3306/kraft_test?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: kraft
    password: kraft1234
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
    session:
      jdbc:
        initialize-schema: always
  thymeleaf:
    cache: false