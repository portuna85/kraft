spring:
  datasource:
    url: jdbc:mysql://localhost:3306/kraft_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: portuna85
    password: Gkstmvns1!

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect   # 메타데이터 의존 회피
  profiles:
    include: oauth

logging:
  level:
    org.hibernate: INFO
