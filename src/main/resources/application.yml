# ========================================
# 1. application.yml (???? ?? ?? ??)
# ========================================
spring:
  profiles:
    active: local   # ?? ??? ????? local

  thymeleaf:
    cache: false     # ??? ?? ???? (?? ? ?? ?? ???)

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080

logging:
  level:
    org.hibernate.SQL: debug                 # ???? SQL ?? ??
    org.hibernate.type.descriptor.sql: trace # ??? ???? ??? ??