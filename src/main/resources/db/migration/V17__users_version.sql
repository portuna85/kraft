-- 비밀번호 변경·정지·탈퇴가 같은 계정 행을 동시에 바꾸면, Hibernate의 기본 변경 감지는 나중에
-- flush되는 쪽이 먼저 것을 조용히 덮어쓴다(개선 보고서 "토큰 소비·재발급·계정 상태 변경의
-- 동시성"). 낙관적 잠금 버전을 두어 그 경우 충돌을 표면화한다 — 이미 PostImage가 같은 목적으로
-- 쓰는 방식과 같다.
--
-- 기존 행은 0부터 시작한다. 저장된 데이터·현재 버전이 바뀌는 컬럼이 아니므로 별도 백필이
-- 필요 없다.
ALTER TABLE users ADD COLUMN version BIGINT NOT NULL DEFAULT 0;
